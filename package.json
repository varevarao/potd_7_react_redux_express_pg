{
  "name": "toolcity",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "NODE_ENV=development DEBUG=toolcity:* node ./bin/www",
    "build": "cd app && NODE_ENV=production npm run build && cp -R build/ ../public/",
    "start": "NODE_ENV=production node ./bin/www"
  },
  "dependencies": {
    "body-parser": "~1.16.0",
    "cookie-parser": "~1.4.3",
    "debug": "~2.6.0",
    "express": "~4.14.1",
    "morgan": "~1.7.0",
    "serve-favicon": "~2.3.2"
  }
}
